# Phase 1 offline-prototype dependencies (ADR-0009: install into a project-local .venv,
# never bare pip / --break-system-packages). See prototype/README.md.
numpy>=1.26
opencv-python>=4.9          # cv2.PyRotationWarper (cylindrical), imdecode, remap, VideoWriter
av>=12.0                    # PyAV: decode HEVC FFMPEGPacket (image_raw/ffmpeg) to RGB
mcap>=1.2                   # read .mcap bags standalone (no ROS install)
mcap-ros2-support>=0.5      # deserialize ROS 2 msgs using schemas embedded in the bag
